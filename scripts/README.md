# scripts/ — Scripts de operación

Utilidades operativas del proyecto (arranque, tareas de mantenimiento, configuración de servicios).

- `nginx/` — configuración de Nginx para el servicio opcional de reverse proxy.
