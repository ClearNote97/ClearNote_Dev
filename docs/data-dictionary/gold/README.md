# Diccionario — schema `gold` (OLAP / productos de analítica)

Tablas y vistas de consumo final (dashboards, modelos, exports). Misma plantilla por tabla que `app/`,
más el **linaje**: de qué fuentes se deriva y con qué job (`admin.job_run` / `job_step`).
