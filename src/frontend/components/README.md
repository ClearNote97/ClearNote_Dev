# src/frontend/components — Componentes reutilizables

Piezas de UI autocontenidas y reutilizables. Sin lógica de negocio; reciben datos por props.
Mobile-first, touch targets ≥44px (ver el sistema de diseño del contrato).
