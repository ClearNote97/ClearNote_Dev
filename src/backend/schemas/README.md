# src/backend/schemas — Esquemas de I/O

Modelos de request/response (validación y serialización). Separados de los modelos de DB
(`src/database/app/models`) para no acoplar la API al esquema físico.
