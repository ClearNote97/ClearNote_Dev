# src/database/analytics — Lado analítico (`gold`)

SQL y definiciones del consumo analítico.

- `queries/` — consultas analíticas reutilizables (OLAP).
- `datasets/` — definiciones de datasets/vistas materializadas de `gold`.
