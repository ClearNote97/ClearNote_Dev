# tests/utils — Tests de utilidades (Polars)

Cubren `src/utils/` (clean_whitespace, format_variables, index_management): happy path,
edge cases (columnas ausentes, valores no convertibles) y estado vacío (DataFrame vacío).
